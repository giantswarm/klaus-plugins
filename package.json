{
  "name": "klaus-plugins",
  "version": "1.0.0",
  "description": "Klaus plugins for Giant Swarm customers",
  "private": true,
  "scripts": {
    "validate": "node shared/scripts/validate-schema.js",
    "check-conflicts": "node shared/scripts/check-conflicts.js",
    "check-version": "node shared/scripts/check-version-bump.js --check",
    "test": "npm run validate && npm run check-conflicts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/giantswarm/klaus-plugins.git"
  },
  "keywords": [
    "klaus",
    "plugins",
    "giant-swarm"
  ],
  "author": "Giant Swarm GmbH",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=16.0.0"
  }
}
